{
  "name": "routines",
  "version": "0.1.0",
  "description": "Unified scheduler + dispatcher for agent routines with global difficulty routing and explicit provider pins.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "routines": "src/cli.ts"
  },
  "scripts": {
    "routines": "bun src/cli.ts",
    "install-shim": "sh scripts/install-shim.sh",
    "e2e": "bash test/e2e.sh",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bash scripts/build-artifact.sh"
  },
  "dependencies": {
    "@sentry/node": "^10.65.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
