# Pinned harness smoke for agy (Gemini). Status paused; fire with `routines run`.
id = "smoke-gemini"
harness = "gemini"
model = "gemini-3.7-flash"
effort = "low"
rrule = "FREQ=YEARLY"
prompt = "You are a smoke test. Reply with exactly this one line and stop: SMOKE_OK harness=gemini"
status = "paused"
timeout_min = 5
pin = true
